# 원본 데이터 (용량 문제로 git 추적 제외)
data/raw/

# 임장 사진 원본 (용량 문제로 git 추적 제외, 1200px 정리본은 docs/fieldwork/photos/에 커밋됨)
docs/fieldwork/*.jpg

# 파이썬 가상환경
venv/

# 파이썬 캐시
__pycache__/
*.pyc

# 주피터 체크포인트
.ipynb_checkpoints/

# OS 관련
.DS_Store
Thumbs.db

# Claude Code 로컬 설정 (개인 권한 설정, 공유 불필요)
.claude/settings.local.json
